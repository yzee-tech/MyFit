enum MuscleGroup {
  Chest
  FrontDelts
  SideDelts
  RearDelts
  Lats
  Traps
  Triceps
  Biceps
  Forearms
  Quads
  Hamstrings
  Glutes
  Calves
  Abs
  Neck
  Adductors
  Abductors
  Custom
}

enum SetType {
  Straight
  V2
  Drop
  Down
  Myorep
  MyorepMatch
  MyorepMatchDown
  TopBackoff
}

enum ChangeType {
  Percentage
  AbsoluteLoad
}

enum WorkoutStatus {
  Skipped
  RestDay
}

enum QuotesDisplayMode {
  PRE_WORKOUT
  POST_WORKOUT
  BETWEEN_SETS
}

/// LEVEL: a machine that shows levels (1, 2, 3…) instead of weights; loads are the level number
enum WeightUnit {
  KG
  LB
  LEVEL
}

/// A routine's unit; ASK means choose it when starting a workout (for routines used at many gyms)
enum RoutineWeightUnit {
  KG
  LB
  ASK
}
